version: "3.7"
services:
  app:
    build:
      context: ./
      dockerfile: ./docker/Dockerfile
    image: processstamps
    container_name: processstamps-app
    restart: unless-stopped
    working_dir: /var/www/
    volumes:
      - ./:/var/www
